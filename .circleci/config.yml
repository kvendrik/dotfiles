version: 2

orbs:
  shellcheck: circleci/shellcheck@1.0.0

jobs:
  lint:
    docker:
      - image: circleci/postgres:9.4.12-alpine
    steps:
      - checkout
      - run:
          name: Linting
          command: ./lint

workflows:
  version: 2
  workflow:
    jobs:
      - lint
