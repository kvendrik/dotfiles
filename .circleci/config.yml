version: 2
jobs:
  lint:
    docker:
      - image: nlknguyen/alpine-shellcheck:v0.4.6
    steps:
      - checkout
      - run:
          name: Linting
          command: ./lint

workflows:
  version: 2
  workflow:
    jobs:
      - lint
